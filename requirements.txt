# Core dependencies for DeadStream
# Install with: pip install -r requirements.txt

# Note: PyQt5 and python-vlc are installed via apt, not pip
# See installation instructions in docs/

# API and networking
requests>=2.31.0

# Configuration
PyYAML>=6.0

# Database (included with Python)
# sqlite3 - built-in

# Development/Testing
pytest>=7.4.0
pytest-qt>=4.2.0
